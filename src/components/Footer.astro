---
import FooterWave from './FooterWave.tsx'
---

<footer>
  <!-- <svg viewBox={`0 0 ${waveWidth} ${waveHeight}`}>
    {Array.from({ length: numLayers }).map((_, i) => (
      <Wave
        key={i}
        width={waveWidth}
        height={waveHeight}
        options={waveOptions}
        pathSuffix={`V ${waveHeight} H 0 Z`}
        duration={[2, 4]}
        className="fill-primary opacity-10 text-red-500"
      />
    ))}
  </svg> -->
  <FooterWave
    client:load
  />
  <div class="relative">
    {Array.from({ length: 2 }).map((_, i) => (
      <div class="bg-primary opacity-10 absolute w-full h-full" />
    ))}
    XXXddddddddddddddddddddddddddddsssssssss dddddddddddd ssssssssssssss
  </div>
</footer>