---
let { id, class: className, ...rest } = Astro.props;
---

<script define:vars={{ id }}>
  // animate the wave to waveB after 1 sec
  // import { genHLines } from "random-shapes"
  console.log(id)

  const waveWidth = 2560
  const waveHeight = 120
  const waveOptions = {
    numLines: 1,
    numControls: 3,
    angleWindowSize: Math.PI / 8,
  }
  
  const waveB = genHLines(waveWidth, waveHeight, waveOptions)[0].curve + `V ${waveHeight} H 0 z`

  // setTimeout(() => {
  //   console.log()
  //   const wave = document.querySelector('footer path')
  //   console.log(wave)

  //   if (wave) {
  //     wave.setAttribute('d', waveB)
  //   }
  // }, 1000)
</script>

<path class={className} style="transition: all 0.5s" />
